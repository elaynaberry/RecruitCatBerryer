@page
@model RecruitCatBerryer.Pages.JobTitles.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>All Job Titles</h1>

<p>
    <a asp-page="Create">Create New Job Title</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JobTitle[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobTitle[0].MinimumSalary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobTitle[0].MaximumSalary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobTitle[0].DegreeRequired)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.JobTitle) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MinimumSalary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaximumSalary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DegreeRequired)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit Job Title</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Job Title Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete Job Title</a>
            </td>
        </tr>
}
    </tbody>
</table>
